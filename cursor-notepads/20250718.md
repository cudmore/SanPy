# 20250718

New day!

In KymRoiAnalysis, we keep a list (actually a dict) of KymRoi. Each KymRoi has a label (key in KymRoiAnalysis dict).

Now, I want to refactor the concept of KymRoi label to ensure it is always a string.

This will affect other backend classes like TifPool. In TifPool we need to switch the 'ROI Number' column to 'ROI Label' and ensure it is a string (it is currentl;y an int)

It will also affect front end guis like TifTreeWindow, etc.

Can you review the changes that need to be made?

# in TifPoolScatter when user selects 'ROI Label' in hue combobox, we get the following except ... any ideas?

```
 INFO sanpy.kym.interface.tif_pool_scatter  tif_pool_scatter.py _on_stat_combobox() line:874 -- name:Hue value:ROI Label
ERROR sanpy.sanpyLogger  sanpyLogger.py handle_exception() line:108 -- Uncaught exception
Traceback (most recent call last):
  File "/Users/cudmore/opt/miniconda3/envs/sanpy-env/lib/python3.12/site-packages/pandas/core/indexes/base.py", line 3812, in get_loc
    return self._engine.get_loc(casted_key)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "pandas/_libs/index.pyx", line 167, in pandas._libs.index.IndexEngine.get_loc
  File "pandas/_libs/index.pyx", line 196, in pandas._libs.index.IndexEngine.get_loc
  File "pandas/_libs/hashtable_class_helper.pxi", line 7088, in pandas._libs.hashtable.PyObjectHashTable.get_item
  File "pandas/_libs/hashtable_class_helper.pxi", line 7096, in pandas._libs.hashtable.PyObjectHashTable.get_item
KeyError: 'ROI Label'

The above exception was the direct cause of the following exception:

Traceback (most recent call last):
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/tif_pool_scatter.py", line 921, in _on_stat_combobox
    self.replot()
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/tif_pool_scatter.py", line 1272, in replot
    uniqueHue = [] if hue is None else df[hue].unique()
                                       ~~^^^^^
  File "/Users/cudmore/opt/miniconda3/envs/sanpy-env/lib/python3.12/site-packages/pandas/core/frame.py", line 4107, in __getitem__
    indexer = self.columns.get_loc(key)
              ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/cudmore/opt/miniconda3/envs/sanpy-env/lib/python3.12/site-packages/pandas/core/indexes/base.py", line 3819, in get_loc
    raise KeyError(key) from err
KeyError: 'ROI Label'
```

# this is still not working

Can you try and load a TifPool from path:

```
/Users/cudmore/Desktop/sanpy-data/analysis-20250618-RHC
```

This path has a number of tif files but no TiffPool or TifTreeWindow csv files saved. I want the TifPool main and mean csv files saved when the TiffPool initially parses this path.

# continue with task

In tifpool I do not any logic that checks the loaded main and mean csv files. If a df column like 'ROI Number' does not exist I do not want any try .. except logic. I just want the code in tifpool to fail.

For example, I do not want error checking code like this:

```
            try:
                self.master_df = pd.read_csv(filepath)
                # Migrate 'ROI Number' to 'ROI Label' if needed (for legacy data)
                if 'ROI Number' in self.master_df.columns and 'ROI Label' not in self.master_df.columns:
                    self.master_df.rename(columns={'ROI Number': 'ROI Label'}, inplace=True)
                    logger.info(f"Migrated 'ROI Number' to 'ROI Label' in loaded pooled data")
                # logger.info(f"Loaded pooled data from: {filepath} ({len(self.master_df)} rows)")
            except Exception as e:
                logger.error(f"Failed to load pooled data from {filepath}: {e}")
                self.master_df = pd.DataFrame()
```

Also, while we are working, DO NOT update the tests. We can turn to the tests at the end as a seperate taks.


# still problems with tif pool constructing and/or saving its main and mean dataframe csv files.

Can you write a simple test function to instantiate a TifPool from the folder '/Users/cudmore/Sites/SanPy/sanpy/kym/sample-data'

and ensure the main and mean csv files are bing saved?

# updating .cursorrules

ok, I found it. I want to update it and want you to follow the new rules.



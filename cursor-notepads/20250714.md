# 20250714

# New Day

# Want to focus on a task in the tif_pool_scatter ScatterWidget.

Yesterday we added an error df to TifPool that is maintained and updated during runtime.

I want a new tab in ScatterWidget that shows the error dataframe as a table. This new 'Errors' tab is added to existing tabs 'Raw' and  'Y-Stat Summary'

Yes, I want the errors table to update live if errors change during runtime. Before you make changes, I want you to really think if they are needed. The 'Y-Stat Summary' data is already updating during runtime, how is that achieved and do we really need any extra code to update the new 'Errors' tab during runtime?

# New focused task

Yesterday we added a 'Tif Pool Scatter' menu action to the main Windows menu. The main Windows menu is SanPyApp wide and not limited to our TifTreeWindow. TifTreeWindow is modifying the main Window menu in _refreshWindowsMenu().

This is not the best strategy as the menu still contains 'Tif Pool Scatter' when other SanPy windows are open (outside the context of TifTreeWindow).

I would suggest you move this new menu action 'Tif Pool Scatter' into the more local main 'Kym' menu (the one that has actions 'Refresh Folder', etc).

# new task

I want to be sure that the TifTreeWindow loads all KymRoiAnalysis into memory when it is instantiated? Does TiFileBackend load all KymRoiAnalysis from disk (csv files) when it is created?

# I want to update this behavior. I would like TifFileBackend to load the saved csv analysis of each KymRoiAnalysis tif file in the folder. I want this csv analysis (from KymRoiAnalysis) to be loaded when the TifFileBackend is created.

- add a bool param to tiffilebackend to control this behavior (default True).
- make sure we have an easy to use API to then load the img data from a given KymRoiAnalysis in TifFileBackemd.

# That is better but we still need to load the img data into KymRoiAnalysis when user double-clicks on a tif file in the list or tableview. Here is a traceback:

```
INFO - sanpy.kym.interface.kym_file_list.tif_tree_window - tif_tree_window.py:onFileDoubleClicked:446 - relative_path:20250312/SSAN/20250312 SSAN R1 LS2.tif.frames/20250312 SSAN R1 LS2 Control.tif
ERROR sanpy.sanpyLogger  sanpyLogger.py handle_exception() line:122 -- Uncaught exception
Traceback (most recent call last):
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/kym_file_list/tif_tree_window.py", line 458, in onFileDoubleClicked
    aWidget = KymRoiWidget(aKymRoiAnalysis)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/kymRoiWidget.py", line 66, in __init__
    self._buildUI()
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/kymRoiWidget.py", line 424, in _buildUI
    self._kymRoiImageWidget = KymRoiImageWidget(self._kymRoiAnalysis, self)
                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/interface/kymRoiImageWidget.py", line 124, in __init__
    self.imgData = self._kymRoiAnalysis.getImageChannel(self._currentChannel)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/cudmore/Sites/SanPy/sanpy/kym/kymRoiAnalysis.py", line 2099, in getImageChannel
    raise RuntimeError(
RuntimeError: Image data not available for channel 0. This can happen in analysis_only mode when trying to access image data. Use load_image_data() to explicitly load image data if needed.
```

# good, that works.

What is the purpose of the TifFileBackend saving and loading tif_file_backend_state.csv? I want to know if this (pandas dataframe) could just be created and maintained during runtime and never saved? Now that TifFileBackend is always loading all KymRoiAnalysis csv files, can't it just create the df for tif_file_backend_state during runtime?

The load_image_data() in KymRoiImageData needs to be called to actually lode the tif image data. IS this called when the user double-clicks. Look at the last stack trace, I do not think load_image_data() is called?

You are getting confused, ym_analysis.loadImgData() does not exist. The proper function is load_image_data(). I fixed it...

```
            # kym_analysis.loadImgData()
            kym_analysis.load_image_data()
```

## I turned off load_analysis_csv=False in TifFileBackend created in TifTreeWindow.

```
        self.backend = TifFileBackend(
            folder_path,
            exclude_folders=["sanpy-reports-pdf"],
            sort_by_grandparent=True,
            load_analysis_csv=False  # abb 20250714 turned off
        )
```

Now when user double-clicks a row in tif table, we get an error:

```
INFO - sanpy.kym.interface.kym_file_list.tif_table_view - tif_table_view.py:_onItemDoubleClicked:473 - fileDoubleClicked -->> emit row:2, column:1 col_name:filename
INFO - sanpy.kym.interface.kym_file_list.tif_tree_window - tif_tree_window.py:onFileDoubleClicked:447 - relative_path:20250312/ISAN/20250312 ISAN R1 LS2.tif.frames/20250312 ISAN R1 LS2 Control.tif
ERROR - sanpy.kym.tif_file_backend - tif_file_backend.py:get_kym_roi_analysis:1829 - Data format error creating KymRoiAnalysis for /Users/cudmore/Dropbox/data/colin/2025/mito-atp/mito-atp-20250623-RHC/20250312/ISAN/20250312 ISAN R1 LS2.tif.frames/20250312 ISAN R1 LS2 Control.tif: KymRoiAnalysis.__init__() got an unexpected keyword argument 'loadAnalysisCsv'
ERROR - sanpy.kym.interface.kym_file_list.tif_tree_window - tif_tree_window.py:onFileDoubleClicked:459 - Failed to load KymRoiAnalysis with image data for: 20250312/ISAN/20250312 ISAN R1 LS2.tif.frames/20250312 ISAN R1 LS2 Control.tif
```


# in TifPool, when the user saves the analysis for one KymRoiAnalysis.
We want to update the master and mean df for just the rows corresponding to the one tif file, see:

```
    def update_from_analysis(self, tif_path: str) -> bool:
```

It seems refresh_df_mean() is updating ALL rows when it should limit its update to rows matching the one tif_path.
```
                # Refresh dfMean after updating master_df
                if len(self.master_df) > 0:
                    self.refresh_df_mean()
                    # Also refresh errors after updating mean data
                    self.refresh_df_errors()
```

I guess in df mean, the rows to update are just rows with the corresponging tif_path.

Does the df mean keep track of the path or relative path for each row?

# When the user save a KymRoiAnalysis with 6 rois, the df mean is calculated 6 times.

It seems the function refresh_df_mean() is called 6 times? Is that correct?

The affected groups are:

```
affected_groups:[('20250312 ISAN R1 LS2', 'FCCP', 1, 0), ('20250312 ISAN R1 LS2', 'FCCP', 2, 0), ('20250312 ISAN R1 LS2', 'FCCP', 3, 0), ('20250312 ISAN R1 LS2', 'FCCP', 4, 0), ('20250312 ISAN R1 LS2', 'FCCP', 5, 0), ('20250312 ISAN R1 LS2', 'FCCP', 6, 0)]
```

```
INFO - sanpy.kym.tif_pool - tif_pool.py:refresh_analysis:1138 - Refreshing analysis for 1 files...
INFO - sanpy.kym.tif_pool - tif_pool.py:refresh_df_mean:678 - Refreshing dfMean for 6 affected groups...
INFO - sanpy.kym.tif_pool - tif_pool.py:refresh_df_mean:679 -   affected_groups:[('20250312 ISAN R1 LS2', 'FCCP', 1, 0), ('20250312 ISAN R1 LS2', 'FCCP', 2, 0), ('20250312 ISAN R1 LS2', 'FCCP', 3, 0), ('20250312 ISAN R1 LS2', 'FCCP', 4, 0), ('20250312 ISAN R1 LS2', 'FCCP', 5, 0), ('20250312 ISAN R1 LS2', 'FCCP', 6, 0)]
INFO - sanpy.kym.tif_pool - tif_pool.py:refresh_df_errors:659 - Refreshing dfErrors...
INFO - sanpy.kym.tif_pool - tif_pool.py:detect_roi_count_mismatches:386 - Detecting ROI count mismatches...
INFO - sanpy.kym.tif_pool - tif_pool.py:detect_roi_count_mismatches:449 - Detected 15 ROI count mismatch errors
```

# when user opens TifPoolScatter, we are getting this exception...

```
            except Exception as e:
                logger.error(f"Failed to open TifPoolScatter: {e}")
                QMessageBox.warning(self, "Error", f"Failed to open TifPoolScatter:\n{str(e)}")
```


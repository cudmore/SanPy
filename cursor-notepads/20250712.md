# 20270712

- In @tif_pool_scatter I am relying on columns in the df to show/hide data in a filtered df.
- in def replot(), I am relying on these boolean columns to reduce/filter the df like:

```
        df = self.getDf()
        # reduce based on show_condition and show_cell
        df = df[df['show_region']]  # include if true
        df = df[df['show_condition']]  # include if true
        df = df[df['show_cell']]  # include if true
        df = df[df['show_roi']]  # include if true
        df = df[df['show_polarity']]  # include if true
        df = df[df['show_epoch']]  # include if true
```

- I want to create a def getFilteredDf() to do similar filtering but I want it to use a dictionary we maintain at runtime.
- This dict is self._filterDict and is created in _buildFilterToobar().
- Basically _filterDict has keys from theseColumns = ['Region', 'Condition', 'Polarity'].
- _filterDict values are a [str] of unique values from a column in the df matching the key str.
- When user toggle a list value like 'SSAN' for key 'Regio' on/off, in _on_condition_checkbox, we want to rebuild the _filterDict [str] values to only include those checkboxes that are selected.

- There is one critical edge case, for the column 'Condition'

    I want the _filterDict key 'Condition' to have values of dict like:

    ```
    {
        'FCCP': ['0', '1'],
        'Control': ['0'],
    }

    Where the [str] list corresponds to checkboxes for 'Repeat' built in _buildFilterToobar()

- Can you try and refactor the code to not use show_region, show_conditon, etc but instead use this new _filterDict?

- To recap, _filterDict should be maintained during runtime and used to filter down the df for plotting in replot()

- We can test this using __main__ of tif_pool_scatter.py which load a df and passes it to ScatterWidget()

## new task

- We toggle a widget to show tables of raw data and a statistical summary of the df.
- We Toggle it in:

```
        elif name == 'Tables':
            self._tabwidget.setVisible(value == QtCore.Qt.Checked)
            # don't replot
            return
```

I want the second tab '        self._tabwidget.addTab(self.yStatSummaryTableWidget, "Y-Stat Summary")'

to show a table of the values in the currently plotted (in replot()) meanDf

Can you suggest changes?

# task 3

- I like those changes but they are not exaclt what I want.
- In your 'key benefits' you say the y-stat summary tab now show 'Grouped by the current hue variable (e.g., by Condition, Region, etc.)'. This is not what I want. You are using getFilteredDf() to get the values for that table which is not correct.
- It is not correct because the actual plotted values are doing additional groupby when we call seaborn function. For example, in the following code, we have a categorical xStat which is 'Region' and a hue=hue (which when we run is 'Condition').

The df generated in getFilteredDf() is sufficient for plotting with seaborn. Do not change our plotting functions. It seems to me we need a special function to generate the df for the stat summary that accounts for the groupby that is automatically done by the seaborn plot?

```
                    sns.pointplot(
                        data=df,
                        x=xStat,
                        y=yStat,
                        # ???
                        #   hue= xStat if hue is None else hue,
                        hue=hue,
                        errorbar=errorbar,  # can be 'se', 'sem', etc
                        # capsize=capsize,
                        linestyle='none',  # do not connect (with line) between categorical x
                        marker="_",
                        markersize=markersize,
                        # markeredgewidth=3,
                        legend=False,
                        # palette=palette,
                        # dodge=0.5,  # separate the points by hue
                        # dodge=None if len(uniqueHue)==1 else 0.5,  # separate the points by hue
                        dodge=dodge,
                        hue_order=hue_order,
                        ax=self.axScatter,
                    )
```

# task #4

When user right click in a scatter table, I want feedback that the table was copied. I want feedback when both the Raw and Y-Stat-Summary are copied. 
How can you do this?

# task 5

in my top toolbar I have a number of widgets including checkboxes for legend, table, and stats. I want to change the order of the checkboxes to: legend, stats, and table

I also want to rename the table checkbox to 'Summary Table', legend to Legend, and stats to Stats.



